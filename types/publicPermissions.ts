import { PublicPermissions as PublicPermissionsModel } from 'server/models';
import { SerializedModel } from './serializedModel';

export type PublicPermissions = SerializedModel<PublicPermissionsModel>;
