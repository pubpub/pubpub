import type { PublicPermissions as PublicPermissionsModel } from 'server/models';

import type { SerializedModel } from './serializedModel';

export type PublicPermissions = SerializedModel<PublicPermissionsModel>;
