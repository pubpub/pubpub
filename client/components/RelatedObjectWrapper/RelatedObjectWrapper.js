import React from 'react';
import PropTypes from 'prop-types';

require('./relatedObjectWrapper.scss');

const RelatedObject = (props) => {
	return null;
};

RelatedObject.propTypes = {

};

export default RelatedObject;