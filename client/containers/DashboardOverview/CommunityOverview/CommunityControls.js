import React from 'react';
// import PropTypes from 'prop-types';

// const propTypes = {
// 	overviewData: PropTypes.object.isRequired,
// };

const CommunityControls = () => {
	return <span />;
};

// CommunityControls.propTypes = propTypes;
export default CommunityControls;
