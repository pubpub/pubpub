/* eslint-disable no-console */
import { Release, merge, PubVersion } from '../../server/models';

export default async () => {
	- Scrape firebase for all public points
	- Correlate with PubVersions, Merges to get createdAt
	

};
